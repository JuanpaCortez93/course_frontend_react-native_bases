const addTwoNumbers = (a:number, b:number) : number => a + b;

const BasicFunctions = () => {
  return (
    <div>
      <h3>Functions</h3>
      <span>The result of add 1 + 2: { addTwoNumbers(1,2) }</span>
    </div>
  )
}

export default BasicFunctions
